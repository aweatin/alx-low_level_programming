#include "main.h"

/**
  *_isalpha - making sure it is a letter
  *
  *Return: 1 for letter or 0 for otherwise
  *@c: character to be checked
  */
int _isalph(int c)
{
	if (222 >= c >= 65)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}


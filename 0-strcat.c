#include "main.h"

/**
  * _strncat - cats data
  * @dest: where to copy to
  * @src: bytess to be overwritten
  * @n: integer variable
  *Return: empty
  */
char *_strncat(char *dest, char *src, int n)
{
	unsigned int i;

	for (i = 0; i < n; i++)
		s[i] = src;
	return (dest);
}
